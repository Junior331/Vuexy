export type ProgressBarProps = {
  bgColor: string;
  width: string;
  height: string;
  progressPorcentage: number;
  invert?: boolean;
  borderRadius: string;
};

export type PBContainerStyledProps = {
  bgColor: string;
  width: string;
  height: string;
  invert?: boolean;
  borderRadius: string;
};

export type PBProgressStyledProps = {
  bgColor: string;
  progressPorcentage: number;
  borderRadius: string;
};
